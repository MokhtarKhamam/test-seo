import { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: "#52c41a",
    colorLink: "#FF0000",
    borderRadius: 20,
  },
};

export default theme;
